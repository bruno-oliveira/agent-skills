# Foundations

## The First Idea

This is Part One of a two-part example book. The part divider before this page carried the title, and because the spec set `strip_h1: true`, this file's own `# Foundations` heading was dropped to avoid repeating it.

The rest is ordinary content — headings, prose, the usual.

## The Second Idea

Two ideas are enough for an example. In a real book each part would be a full document.
